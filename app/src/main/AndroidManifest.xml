<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reciprice">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RecipriceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_reciprice"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_reciprice_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.RecipeListActivity" />
        <activity android:name=".ui.IngredientActivity" />
        <activity android:name=".ui.FindGroceryStoreActivity" />
        <activity android:name=".ui.PriceActivity"></activity>
    </application>

</manifest>